services:
  db:
    image: postgis/postgis:15-3.3
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=gis_password
      - POSTGRES_DB=smartcity_gis
    ports:
      - "5433:5432"
    volumes:
      - postgis_data:/var/lib/postgresql/data
volumes:
  postgis_data: